name: MCP

on: [push, pull_request]

jobs:
  mcp:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Context7 MCP Connector
      uses: context7/mcp-connector-action@v1
      with:
        mcp-url: ${{ secrets.MCP_URL }}
        mcp-token: ${{ secrets.MCP_TOKEN }}